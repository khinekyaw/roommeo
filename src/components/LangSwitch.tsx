import React from 'react'
import { Button } from '@nextui-org/button'

const LANGUAGES = [
  {
    id: 'en',
    title: 'English',
  },
  {
    id: 'mm',
    title: 'Myanmar',
  },
]

const LangSwitch = () => {
  return <Button variant='ghost'>Mm</Button>
}

export default LangSwitch
